    // NÃO consegui "testar" esse código e NÃO entendi muito bem pra que isso serve, VER Depois !! 
describe('Teste', () => {
    it('Deve ser somado 1+1', () => {
        expect(1+1).toBe(2);
    })
})