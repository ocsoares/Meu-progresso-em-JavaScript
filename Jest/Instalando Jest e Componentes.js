    // Instalando Jest e Componentes !!
    //  OBS: Se for em TypeScript, precisa ter o tsconfig (se não tiver, tsc --init) !!!
    
// npm install --save-dev jest
// npm install @types/jest
// npm install ts-jest

// Instalar a Extensão Jest Runner !!

// Para Iniciar com Jest = npx jest --init

// Descomentar o Código testMatch !!

// Criar o Arquivo para Testes = Nome do Arquivo.test.extensão !!